import React from 'react';
import { Text, View, Modal } from 'react-native';


const ScheduleModal = () => {
    return(
        <Modal>
            <Text>Hey</Text>
        </Modal>
    );
};

export default ScheduleModal;